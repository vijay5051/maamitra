name: Build Native App (Android / iOS)

# Manual trigger from the GitHub Actions UI: pick a platform and an
# optional auto-submit flag. EAS Build runs on Expo's infra (cloud), so
# this workflow just needs an EXPO_TOKEN — env vars come from the EAS
# production environment automatically.

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform'
        required: true
        default: 'android'
        type: choice
        options: [android, ios, all]
      profile:
        description: 'Build profile'
        required: true
        default: 'production'
        type: choice
        options: [production, preview, development]
      submit:
        description: 'Auto-submit to store after build (production only)'
        required: false
        default: false
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      EAS_BUILD_NO_EXPO_GO_WARNING: 'true'
    steps:
      # F4 — third-party actions pinned to commit SHA (Codecov-2021 defense).
      # Bump the SHA + the comment together; never let them drift.
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4.4.0
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup EAS CLI
        uses: expo/expo-github-action@c7b66a9c327a43a8fa7c0158e7f30d6040d2481e  # v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build (with optional submit)
        run: |
          SUBMIT_FLAG=""
          if [ "${{ inputs.submit }}" = "true" ] && [ "${{ inputs.profile }}" = "production" ]; then
            SUBMIT_FLAG="--auto-submit"
          fi
          npx eas-cli build \
            --platform ${{ inputs.platform }} \
            --profile ${{ inputs.profile }} \
            --non-interactive \
            --no-wait \
            $SUBMIT_FLAG
